#include<iostream>
using namespace std;
int main()
{
    //Define two variables to store inputs
    int num1;
    int num2;
    //Reading two values and store them in your two variables
    cin>>num1;
    cin>>num2;

    //display sum of numbers here using long so we create constraints and save memory
    cout<<(long)num1 + (long)num2 << endl; 
}